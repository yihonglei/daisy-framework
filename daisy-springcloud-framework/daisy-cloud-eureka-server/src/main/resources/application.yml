server:
  port: 9000
spring:
  application:
    name: dasiy-cloud-eureka-server
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: true